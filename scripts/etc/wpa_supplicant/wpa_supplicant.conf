country=GB
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
network={
        ssid="{{ssid}}"
        psk="{{psk}}"
        key_mgmt={{key_mgmt}}
        proto={{proto}}
        pairwise={{pairwise}}
        auth_alg={{auth_alg}}
}